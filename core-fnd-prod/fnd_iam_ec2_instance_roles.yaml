AWSTemplateFormatVersion: 2010-09-09
Description: Create base IAM roles

Resources:

  # Roles
  CoreFndProdNessus:
    Type: AWS::IAM::Role
    Properties:
        AssumeRolePolicyDocument:
            Version: '2012-10-17'
            Statement:
              -
                Effect: Allow
                Principal:
                  Service: ec2.amazonaws.com
                Action: sts:AssumeRole
        ManagedPolicyArns:
            - arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
        RoleName: core-fnd-prod-nessus

  # Instance Profiles
  CoreFndProdNessusProfile:
    Type: AWS::IAM::InstanceProfile
    Properties: 
      Roles:
        - !Ref CoreFndProdNessus
      InstanceProfileName: core-fnd-prod-nessus-profile