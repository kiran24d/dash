AWSTemplateFormatVersion: 2010-09-09
Description: Create base IAM roles

Resources:

  # Roles
  CoreCdtProdNessus:
    Type: AWS::IAM::Role
    Properties:
        AssumeRolePolicyDocument:
            Version: '2012-10-17'
            Statement:
              -
                Effect: Allow
                Principal:
                  Service: ec2.amazonaws.com
                Action: sts:AssumeRole
        ManagedPolicyArns:
            - arn:aws:iam::aws:policy/AmazonEC2ReadOnlyAccess
        RoleName: core-cdt-prod-nessus

  # Instance Profiles
  CoreCdtProdNessusProfile:
    Type: AWS::IAM::InstanceProfile
    Properties: 
      Roles:
        - !Ref CoreCdtProdNessus
      InstanceProfileName: core-cdt-prod-nessus-profile