---
AWSTemplateFormatVersion: '2010-09-09'
Description: ICR-3139, IAM users for Kapow cp to read/write to S3
Metadata: {}

Parameters:

  AccountName:
    Type: String
    Description: Name of the account this has been deployed on
    Default: core-kapow-prod

Resources:

  CoreKapowProdS3CpUser:
    Type: AWS::IAM::User
    Properties:
      Path: /
      UserName:
        Fn::Join:
          - "-"
          - - !Ref AccountName
            - kpw-s3-cp

  CoreKapowProdS3CpPolicy:
    Type: AWS::IAM::Policy
    Properties:
      PolicyName: core-kapow-prod-s3-cp-policy
      Users:
        - !Ref CoreKapowProdS3CpUser
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          -
            Effect: "Allow"
            Action:
              - s3:Get*
              - s3:List*
              - s3:PutObject
            Resource:
              - "arn:aws:s3:::core-kapow-prod-cp-assets-us-east-1/*"
              - "arn:aws:s3:::core-kapow-prod-cp-assets-us-east-1"
